# tetris / casual games

Static web games + small Python helpers (`serve.py`, `portctl.py`).

## Conventions
- Python: `#!/usr/bin/env python3`, `from __future__ import annotations`, type hints, `main()` + `__main__` guard.
- Keep new demos as a single root-level script unless asked otherwise.
- Do not touch unrelated dirty files under `casual/`.
