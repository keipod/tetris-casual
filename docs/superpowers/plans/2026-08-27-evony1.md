# Evony Age I Implementation Plan

> **For agentic workers:** Implement task-by-task. Checkboxes track progress.

**Goal:** Ship a playable Evony Age I clone on the casual portal (Server 1), with economy + military + shared map core first.

**Architecture:** Python WS+SQLite authoritative server; Canvas+DOM client; hub card + ports.json.

**Tech Stack:** Python 3.10+, stdlib HTTP/WS, SQLite, vanilla JS Canvas, pytest

**Spec:** `docs/superpowers/specs/2026-08-27-evony1-design.md`

## Global Constraints

- Path `casual/evony1/`, port **48951**, timer **1×**
- Identity via `device_id` in localStorage; reset deletes server-side lord
- Do not modify other agents’ dirty game trees
- No OAuth; no kie.ai unless user approves cost
- Korean UI copy ok in chat; game strings via i18n EN/KO

---

### Task 1: Data + engine core (economy)

**Files:**
- Create: `casual/evony1/data.py`, `casual/evony1/engine.py`, `casual/evony1/store.py`
- Test: `casual/evony1/tests/test_engine.py`

**Produces:** `tick()`, `create_lord()`, `build()`, `upgrade()`, `snapshot_for()`, resource formulas

- [ ] Failing tests for tick production + build complete
- [ ] Implement data tables + engine + sqlite store
- [ ] pytest green

### Task 2: WebSocket server + scaffold client

**Files:**
- Create: `casual/evony1/server.py`, `index.html`, `style.css`, `game.js`, `i18n.js`, `render.js`
- Modify: `ports.json`, `casual/index.html`, `.gitignore`, `docs/summary.md`

**Produces:** hello/snapshot/build/upgrade/chat/reset; Server 1 lobby; TOWN/CITY/MAP tabs

- [ ] server serves static + WS
- [ ] hub card + ports entry
- [ ] CDP: hub → card → Server 1 → snapshot UI

### Task 3: Military + map

**Files:** extend `engine.py`, `server.py`, `game.js`, `render.js`; tests for combat/march

**Produces:** train, march to NPC, reports, map_query window, world chat

- [ ] pytest combat + march ETA
- [ ] CDP attack NPC → report

### Task 4: Research, WoF, multi-city hooks, polish

**Produces:** research queue, spin_wheel daily, second city unlock stub, FX

- [ ] pytest research complete
- [ ] CDP vision downsample + airouter qwen3-vl layout check
